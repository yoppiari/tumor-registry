import { PrismaService } from '@/common/database/prisma.service';
import { DatabaseBackupStrategy } from '../strategies/database-backup.strategy';
import { BackupStatistics, BackupHealthStatus, RecoveryOptions, RecoveryResult } from '../interfaces/backup.interface';
import { CreateBackupJobDto } from '../dto/create-backup-job.dto';
export declare class BackupService {
    private readonly prisma;
    private readonly databaseBackupStrategy;
    private readonly logger;
    private scheduledJobs;
    constructor(prisma: PrismaService, databaseBackupStrategy: DatabaseBackupStrategy);
    createBackupJob(createBackupJobDto: CreateBackupJobDto): Promise<any>;
    executeBackup(backupJobId: string): Promise<any>;
    restoreFromBackup(executionId: string, options: RecoveryOptions): Promise<RecoveryResult>;
    getBackupJobs(filters?: {
        backupType?: string;
        dataSource?: string;
        isActive?: boolean;
        centerId?: string;
    }): Promise<any[]>;
    getBackupExecutions(filters?: {
        backupJobId?: string;
        status?: string;
        startDate?: Date;
        endDate?: Date;
        limit?: number;
    }): Promise<any[]>;
    getBackupStatistics(centerId?: string): Promise<BackupStatistics>;
    getBackupHealthStatus(centerId?: string): Promise<BackupHealthStatus>;
    deleteBackupExecution(executionId: string): Promise<void>;
    deleteBackupJob(jobId: string): Promise<void>;
    private initializeScheduledBackups;
    private scheduleBackupJob;
    private generateBackupFilePath;
    private validateStorageLocation;
    private verifyBackupIntegrity;
    private updateBackupJobStats;
    private logRestoreOperation;
    private scheduleCleanup;
    private calculateStorageUsed;
    private calculateAverageBackupTime;
    private getLastBackupTime;
    private getNextScheduledBackup;
    private getStorageDistribution;
    private getFailedBackupCount;
    private getLastSuccessfulBackup;
    private getStorageCapacity;
    private getUpcomingBackups;
    private generateBackupAlerts;
    private estimateBackupDuration;
}
