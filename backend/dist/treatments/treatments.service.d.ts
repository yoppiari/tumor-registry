import { Model } from 'mongoose';
import { TreatmentPlan, TreatmentSession, TreatmentProtocol, MedicalRecord, QualityMetrics, TreatmentReport } from './interfaces/treatment.interface';
import { CreateTreatmentPlanDto } from './dto/create-treatment-plan.dto';
import { CreateTreatmentSessionDto } from './dto/create-treatment-session.dto';
import { UpdateTreatmentPlanDto } from './dto/update-treatment-plan.dto';
import { SearchTreatmentDto } from './dto/search-treatment.dto';
import { CreateMedicalRecordDto } from './dto/create-medical-record.dto';
import { GenerateReportDto } from './dto/generate-report.dto';
export declare class TreatmentsService {
    private treatmentPlanModel;
    private treatmentSessionModel;
    private treatmentProtocolModel;
    private medicalRecordModel;
    private qualityMetricsModel;
    private treatmentReportModel;
    constructor(treatmentPlanModel: Model<TreatmentPlan>, treatmentSessionModel: Model<TreatmentSession>, treatmentProtocolModel: Model<TreatmentProtocol>, medicalRecordModel: Model<MedicalRecord>, qualityMetricsModel: Model<QualityMetrics>, treatmentReportModel: Model<TreatmentReport>);
    createTreatmentPlan(createTreatmentPlanDto: CreateTreatmentPlanDto, createdBy: string): Promise<TreatmentPlan>;
    findAllTreatmentPlans(searchDto: SearchTreatmentDto): Promise<{
        treatmentPlans: TreatmentPlan[];
        total: number;
        page: number;
        limit: number;
        totalPages: number;
        hasNext: boolean;
        hasPrevious: boolean;
    }>;
    findTreatmentPlanById(id: string): Promise<TreatmentPlan>;
    updateTreatmentPlan(id: string, updateTreatmentPlanDto: UpdateTreatmentPlanDto, updatedBy: string): Promise<TreatmentPlan>;
    activateTreatmentPlan(id: string, activatedBy: string): Promise<TreatmentPlan>;
    completeTreatmentPlan(id: string, completedBy: string): Promise<TreatmentPlan>;
    createTreatmentSession(createTreatmentSessionDto: CreateTreatmentSessionDto, createdBy: string): Promise<TreatmentSession>;
    findTreatmentSessionsByPlan(treatmentPlanId: string): Promise<TreatmentSession[]>;
    completeTreatmentSession(sessionId: string, postAssessmentData: any, completedBy: string): Promise<TreatmentSession>;
    private updateTreatmentPlanProgress;
    private calculateAdherenceScore;
    createMedicalRecord(createMedicalRecordDto: CreateMedicalRecordDto, createdBy: string): Promise<MedicalRecord>;
    findMedicalRecordsByPatient(patientId: string, limit?: number): Promise<MedicalRecord[]>;
    calculateQualityMetrics(treatmentPlanId: string): Promise<QualityMetrics>;
    private calculateGuidelineConcordance;
    private calculateRelativeDoseIntensity;
    generateTreatmentReport(generateReportDto: GenerateReportDto, generatedBy: string): Promise<TreatmentReport>;
    private generateTreatmentSummaryReport;
    private generateProgressReport;
    private generateOutcomeAnalysisReport;
    private generateQualityMetricsReport;
    private generateAdverseEventsReport;
    private calculateAverageTreatmentDuration;
    private generateReportSummary;
    private generateReportInsights;
    private generateReportRecommendations;
}
